apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopwave
  namespace: ecommerce
  labels:
    app: shopwave
    version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: shopwave
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0      # Zero-downtime deployments
  template:
    metadata:
      labels:
        app: shopwave
    spec:
      # Security context
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000

      containers:
        - name: shopwave
          # ⚠️ GitHub Actions updates this image tag automatically on each push
          image: rkdasari0318/shopwave:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
              name: http
          
          env:
            - name: FLASK_ENV
              value: "production"
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: secret-key

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"

          # Health probes
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3

      restartPolicy: Always