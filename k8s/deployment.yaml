apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopwave
  namespace: shopwave
spec:
  replicas: 2
  selector:
    matchLabels:
      app: shopwave
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: shopwave
    spec:
      containers:
        - name: shopwave
<<<<<<< HEAD
          # ↓ GitHub Actions updates this tag automatically on every push
          image: rkdasari0318/shopwave:sha-073c870
=======
          # ⚠️ GitHub Actions updates this image tag automatically on each push
          image: rkdasari0318/shopwave:sha-073c870
>>>>>>> d65fd337be6ad1e78044a05898e0e4b84692bd43
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10