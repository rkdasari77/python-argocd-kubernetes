apiVersion: v1
kind: Service
metadata:
  name: shopwave-svc
  namespace: shopwave
spec:
  selector:
    app: shopwave
  ports:
    - port: 80
      targetPort: 5000
  type: LoadBalancer          # Azure gives this a public IP automatically